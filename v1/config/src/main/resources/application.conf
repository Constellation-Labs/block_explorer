block-explorer {
  sqs {
    url = ""
  }
  elasticsearch {
    url = ""
    port = "80"
    indexes {
      snapshots = "snapshots"
      transactions = "transactions"
      checkpoint-blocks = "checkpoint-blocks"
    }
  }
}